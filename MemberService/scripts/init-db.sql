-- Init database script for MemberService

-- Create database (if not using docker-compose)
-- CREATE DATABASE memberservice_dev;

-- Create user (if not using docker-compose)
-- CREATE USER memberservice WITH PASSWORD 'Dev@Password123';
-- GRANT ALL PRIVILEGES ON DATABASE memberservice_dev TO memberservice;

-- The database schema will be created by EF Core migrations
-- Run: dotnet ef database update