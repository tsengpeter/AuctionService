# 開發進度總結 — 2026-04-12

**分支狀態**：`002-member-module` → merged to `develop` → merged to `master`  
**測試結果**：Unit 144 passed / Integration 41 passed / Build 0 errors

---

## 今日完成項目

### 1. Member 模組完整實作（002-member-module）

所有 80 個任務全部完成，涵蓋 Phase 1–10：

| Phase | 內容 | 狀態 |
|-------|------|------|
| Phase 1 | Setup — BCrypt.Net-Next、RateLimiter NuGet | ✅ |
| Phase 2 | Domain、Application Abstractions、EF Core、Infrastructure DI | ✅ |
| Phase 3 | US1 Register（Command + Validator + Handler + Tests） | ✅ |
| Phase 4 | US2 Login（Command + Handler + Rate Limiting + Tests） | ✅ |
| Phase 5 | US3 RefreshToken（Token Rotation + Tests） | ✅ |
| Phase 6 | US4 Logout（冪等撤銷 + Tests） | ✅ |
| Phase 7 | US5 GetMe + GetPhoneCountryCodes（Query + Tests） | ✅ |
| Phase 8 | US6 UpdateProfile（Command + Tests） | ✅ |
| Phase 9 | US7 ChangePassword（Command + Tests） | ✅ |
| Phase 10 | Polish — CorrelationId、RefreshTokenCleanupService、Swagger、HealthCheck | ✅ |

**實作細節：**
- `AuthController`（`/api/auth/*`）、`UsersController`（`/api/users/*`）、`PhoneCountryCodesController`
- `JwtTokenService`（HS256, AccessToken 15min, RefreshToken Token Rotation）
- `BcryptPasswordHasher`（BCrypt.Net-Next, WorkFactor=12）
- `RefreshTokenCleanupService`（Background Service，定期清理過期 Token）
- EF Core Migration（`member` schema：`users`、`refresh_tokens`、`phone_country_codes` 表）
- 登入失敗計數器（IMemoryCache）+ Rate Limiter（IP 限速 10 req/min）

### 2. Program.cs 自動 Migration Runner

啟動時自動對所有模組 DbContext 執行 `MigrateAsync()`：
```csharp
// 5 個 DbContext：Member / Auction / Bidding / Ordering / Notification
await db.Database.MigrateAsync();
```
**效果**：`docker compose up` 後 API 自動建立全部 schema，無需手動執行 `dotnet ef database update`。

### 3. Docker 全端運行驗證

執行 `docker compose -f docker-compose.yml -f docker-compose.full.yml up -d --build`：

| 容器 | 狀態 | Port |
|------|------|------|
| `auctionservice-postgres` | healthy | 5432 |
| `auctionservice-api` | healthy | 8080 |
| `auctionservice-pgadmin` | up | 5050 |

Migration 自動執行成功，`GET /health` 回傳：
```json
{
  "status": "Healthy",
  "entries": {
    "member-db": { "status": "Healthy" },
    "auction-db": { "status": "Healthy" },
    "bidding-db": { "status": "Healthy" },
    "ordering-db": { "status": "Healthy" },
    "notification-db": { "status": "Healthy" }
  }
}
```

### 4. 文件更新

- `specs/002-member-module/tasks.md` — 所有 80 個任務標記為 `[x]`
- `requirmentspec/implementation-checklist.md` — Phase 2（2-B～2-E）全部標記為 `[x]`

### 5. 分支合併流程

```
002-member-module → develop → master
```

- Build：0 errors（8 NU1603 warnings，無害）
- Unit Tests：144 passed / 0 failed（master 驗證）
- Integration Tests：41 passed / 0 failed（master 驗證，Testcontainers PostgreSQL）

---

## 待完成項目

### Phase 2-E（小缺口）
- [ ] implementation-checklist.md Phase 2-E 的 PR 建立/Merge 步驟紀錄仍為 `[ ]`（實際已完成，僅文件未標記）

---

## 下一步：Phase 3 — Auction 模組

**分支**：`003-auction-module`（from `master`）

### 建立分支
```bash
git checkout master
git pull origin master
git checkout -b 003-auction-module
```

### 功能範圍（依 implementation-checklist.md Phase 3）

| 功能 | 端點 |
|------|------|
| 建立拍賣商品 | `POST /api/auctions` |
| 編輯草稿商品 | `PUT /api/auctions/{id}` |
| 上架商品（Draft→Active） | `PATCH /api/auctions/{id}/publish` |
| 商品列表（分類/關鍵字/分頁） | `GET /api/auctions` |
| 商品詳情（含最高出價） | `GET /api/auctions/{id}` |
| 加入/移除追蹤清單 | `POST/DELETE /api/auctions/{id}/watchlist` |
| 查詢追蹤清單 | `GET /api/watchlist` |

### 商品狀態機
```
Draft → Active → Ended
```

### 技術重點
- `AuctionEndBackgroundService`（PeriodicTimer 60 秒，掃描過期 Active 商品）
- `AuctionWonEvent : IDomainEvent`（AuctionId, WinnerId, SoldAmount, SellerId）供 Bidding/Ordering 模組消費
- `IBiddingQueryService` 介面隔離跨模組查詢
- EF Core：`auction` schema，`auctions`、`auction_images`、`categories`、`watchlist` 表

### TDD 重點
- `AuctionStatusMachineTests` — Draft→Active→Ended 狀態流轉
- `AuctionEndServiceTests` — 結標時 `AuctionWonEvent` 正確發布
- `CreateAuctionCommandTests` — end_time > now，否則 422

### SpecKit 流程
```
/speckit.specify 實作 Auction 模組：...（見 implementation-checklist.md Phase 3-B）
/speckit.clarify
/speckit.plan
/speckit.tasks
/speckit.implement
/speckit.checklist
```

---

## 整體進度

| Phase | 模組 | 狀態 |
|-------|------|------|
| Phase 0 | Git 初始化 | ✅ 完成 |
| Phase 1 | Backend Scaffold | ✅ 完成 |
| Phase 2 | Member 模組 | ✅ 完成 (2026-04-12) |
| Phase 3 | Auction 模組 | ⏳ **下一個** |
| Phase 4 | Bidding 模組 | ⏳ 待開始 |
| Phase 5 | Ordering 模組 | ⏳ 待開始 |
| Phase 6 | Notification 模組 | ⏳ 待開始 |

**完成比例**：2 / 6 模組（33%）  
**測試基線**：185 tests（Unit 144 + Integration 41），全綠
